import { lookup } from "dns";

export enum Lookup {
  Roles = 'roles',
  Students = 'students',
  Subjects = 'subjects',
  Users = 'user/data',
  Schools = 'schools',
  Topics = 'topics',
  Groups = 'groups',
  ClassRooms = 'classRooms',
}
